{
  "private": true,
  "type": "module",
  "name": "an-ki-demo",
  "description": "Static cluster simulation for the an-ki distributed neural network",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 8000"
  }
}
